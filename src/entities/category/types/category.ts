type Category = {
    id: string
    imageUrl?: string,
    title: string,
    smallTitle?: string,
    smallIcon?: string,
    color: string,
    slug: string,
    parentId?: string
};

export type { Category }